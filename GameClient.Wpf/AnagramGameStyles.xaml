<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Brushes -->
    <SolidColorBrush x:Key="AnagramCardBackgroundBrush" Color="#111827" />
    <SolidColorBrush x:Key="AnagramCardBorderBrush" Color="#334155" />
    <SolidColorBrush x:Key="AnagramAccentBrush" Color="#38BDF8" />
    <SolidColorBrush x:Key="AnagramAccentSoftBrush" Color="#0EA5E9" />
    <SolidColorBrush x:Key="AnagramTextBrush" Color="#E5E7EB" />
    <SolidColorBrush x:Key="AnagramSecondaryTextBrush" Color="#9CA3AF" />
    <SolidColorBrush x:Key="AnagramDividerBrush" Color="#1F2933" />

    <!-- Input brushes (used by code-behind to reset) -->
    <SolidColorBrush x:Key="AnagramInputBorderBrush" Color="#4B5563" />
    <SolidColorBrush x:Key="AnagramInputForegroundBrush" Color="#E5E7EB" />

    <!-- Options panel -->
    <Style x:Key="AnagramOptionsBorder" TargetType="Border">
        <Setter Property="Background" Value="#020617" />
        <Setter Property="BorderBrush" Value="#1F2937" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="AnagramOptionsHeader" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{StaticResource AnagramTextBrush}" />
    </Style>

    <Style x:Key="AnagramOptionsLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{StaticResource AnagramSecondaryTextBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- Buttons -->
    <Style x:Key="AnagramPrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AnagramAccentBrush}" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource AnagramAccentSoftBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AnagramSecondaryButton" TargetType="Button" BasedOn="{StaticResource AnagramPrimaryButton}">
        <Setter Property="Background" Value="#111827" />
        <Setter Property="Foreground" Value="{StaticResource AnagramTextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource AnagramAccentBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#0B1120" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Letters + timer -->
    <Style x:Key="AnagramLettersText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="32" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{StaticResource AnagramAccentBrush}" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="Margin" Value="0,0,0,2" />
    </Style>

    <Style x:Key="AnagramTimerText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource AnagramTextBrush}" />
        <Setter Property="TextAlignment" Value="Center" />
    </Style>

    <!-- Divider -->
    <Style x:Key="AnagramDivider" TargetType="Separator">
        <Setter Property="Margin" Value="0,8,0,8" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="{StaticResource AnagramDividerBrush}" />
        <Setter Property="Height" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Background="{TemplateBinding Background}"
                            Height="{TemplateBinding Height}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Player panels -->
    <Style x:Key="AnagramPlayerPanel" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource AnagramCardBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource AnagramCardBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="AnagramPlayerHeader" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource AnagramTextBrush}" />
    </Style>

    <Style x:Key="AnagramScoreLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{StaticResource AnagramSecondaryTextBrush}" />
    </Style>

    <Style x:Key="AnagramScoreValue" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{StaticResource AnagramAccentBrush}" />
    </Style>

    <!-- Words list -->
    <Style x:Key="AnagramWordsListStyle" TargetType="ItemsControl">
        <Setter Property="Margin" Value="0,4,0,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ItemsControl">
                    <ItemsPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{Binding}"
                               Margin="0,1,0,1"
                               Foreground="{StaticResource AnagramTextBrush}"
                               FontSize="13" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Word input -->
    <Style x:Key="AnagramWordInputBox" TargetType="TextBox">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource AnagramInputForegroundBrush}" />
        <Setter Property="Background" Value="#020617" />
        <Setter Property="BorderBrush" Value="{StaticResource AnagramInputBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="CaretBrush" Value="{StaticResource AnagramAccentBrush}" />
    </Style>

    <!-- Status text -->
    <Style x:Key="AnagramStatusText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{StaticResource AnagramSecondaryTextBrush}" />
        <Setter Property="TextAlignment" Value="Center" />
    </Style>

    <!-- Shake animation storyboard -->
    <Storyboard x:Key="AnagramShakeStoryboard">
        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
            <EasingDoubleKeyFrame KeyTime="0:0:0" Value="0" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.05" Value="-6" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.10" Value="6" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="-4" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.20" Value="4" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="-2" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.30" Value="2" />
            <EasingDoubleKeyFrame KeyTime="0:0:0.35" Value="0" />
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>
</ResourceDictionary>
