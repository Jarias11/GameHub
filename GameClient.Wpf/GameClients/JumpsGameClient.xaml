<UserControl x:Class="GameClient.Wpf.GameClients.JumpsGameClient"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
			 Focusable="True"
			 Loaded="UserControl_Loaded">

	<Grid Background="#111111">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Header / instructions + score -->
		<Grid Margin="8">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<!-- Left: game name + instructions -->
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="Jumps"
						   FontSize="20"
						   FontWeight="Bold"
						   Foreground="White"/>
				<TextBlock Text="A and D to choose starting position, Space to jump (starts),"
						   Margin="8,0,0,0"
						   VerticalAlignment="Center"
						   Foreground="LightGray"
						   FontSize="12"/>
			</StackPanel>

			<!-- Center: Level -->
			<TextBlock x:Name="LevelText"
					   Grid.Column="1"
					   Text="Level 1"
					   Foreground="White"
					   VerticalAlignment="Center"
					   HorizontalAlignment="Center"
					   FontSize="14"
					   Margin="8,0,8,0"/>

			<TextBlock x:Name="ScoreText"
					   Grid.Column="2"
					   Text="Score: 0 (Best: 0)"
					   Foreground="White"
					   VerticalAlignment="Center"
					   FontSize="14"
					   Margin="8,0,0,0"/>

			<!-- Debug (scroll + player speed) -->
			<TextBlock x:Name="DebugText"
					   Grid.Column="3"
					   Text="Scroll: 0  PlayerMax: 0"
					   Foreground="LightGray"
					   VerticalAlignment="Center"
					   FontSize="12"
					   Margin="12,0,0,0"/>
		</Grid>


		<!-- Game area -->
		<Border Grid.Row="1"
				Margin="8"
				BorderBrush="#444444"
				BorderThickness="1"
				CornerRadius="4"
				Background="#222222">

			<!-- Skia rendering surface -->
			<skia:SKElement x:Name="SkElement"
							Width="480"
							Height="480"
							PaintSurface="OnPaintSurface"/>
		</Border>
	</Grid>
</UserControl>
